#include "semantic.h"

void Semantic::push_id(const std::string& id, const std::string& type) {
    
}

const std::string& check_id(const std::string& id) {
    // if (TID.find(id) != TID.end()) {
    //     throw std::logic_error("Variable");
    // }
}