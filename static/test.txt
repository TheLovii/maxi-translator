import math

int a, b, c = 3;

class Sample{
	Sample(int x, int y, List<int> f) {
	    this.x = x;
		this.y = y;
		this.onAwake += f;
	}
	property int a;
	public func get_max() -> List<int> {
        return this.onAwake(this.x, this.y);
	}
}